## subconverter自行修改记录

1. 文件base/pref.toml:
最后一行添加：
```
app_delay_exit = 100
```

2. 文件src/main.cpp:
添加了一个exit退出程序的请求
```
// 3秒钟退出程序
webServer.append_response("GET", "/exit", "text/plain;charset=utf-8", [](RESPONSE_CALLBACK_ARGS) -> std::string
{    
    // Create a detached thread that sleeps for 3 seconds then exits the program 
    std::thread([](){
        std::this_thread::sleep_for(std::chrono::seconds(3));
        exit(0);
    }).detach();

    // Immediately response to the request
    return "subconverter将在3秒后退出!";
});
```
使用方法:
```
http://127.0.0.1:25500/exit
```

3. 添加```自定义-直连.list```规则文件:
  - ```自定义-直连.list```规则文件在subconverter/rules目录下创建。
  - ```自定义-直连.list```规则文件中添加需要直连的站点。
  - 将```自定义-直连.list```规则文件添加到```subconverter/base/snippets/rulesets.txt```文件中，如下:
    ```
    //subconverter/snippets/rulesets.txt
    🎯 全球直连,rules/自定义-直连.list

    //subconverter/snippets/rulesets.toml
    [[rulesets]]
    group = "🎯 全球直连"
    ruleset = "rules/自定义-直连.list"
    ```




##
## Github Action修改
1. 修改build.yml执行条件:
修改前:
```
on:
  push:
    branches: [ master ]
    tags:
      - '**'
  workflow_dispatch:
  pull_request:

```
修改后:
```
on:
  push:
    branches: [ master ]
  #   tags:
  #     - '**'
  # workflow_dispatch:
  # pull_request:
```

2. 还修改了一些任务执行条件，具体查看build.yml文件



##
## 文件备份
1. 旧版本的subconverter_v0.9.3备份:
```
backup/subconverter-old-v0.9.3.zip
```